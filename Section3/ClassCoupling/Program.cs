using System;

namespace ClassCoupling
{
    class ProgramClassCoupling
    {
        static void Main(string[] args)
        {
            // Coupling - a measure of how interconnected classes and subsystems are
            // Tightly coupled - an app whose classes are closely related or dependant on each others. BAD!
            // Loosely Coupled - ideal. we want to minimize changes. when we change one class we don't want that change to affect other parts

            // How?
            // -Encapsulation
            // -The relationship between classes
            // -Interfaces

            // Inheritance
            // Composition
            // Composition > Inheritance?
        }
    }
}
